import fs from 'node:fs/promises';const root=new URL('../',import.meta.url),dist=new URL('dist/',root);await fs.rm(dist,{recursive:true,force:true});await fs.mkdir(dist,{recursive:true});for(const f of ['index.html','style.css','src','kernels','generated','vendor','tests','LICENSE','README.md'])await fs.cp(new URL(f,root),new URL(f,dist),{recursive:true});console.log('Static site written to dist/. Serve over HTTPS or localhost.');
